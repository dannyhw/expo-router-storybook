import { View } from "react-native";
import StorybookUI from "../.storybook";

export default function Storybook() {
  return (
    <View style={{ flex: 1 }}>
      <StorybookUI />
    </View>
  );
}
